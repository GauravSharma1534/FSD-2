# Frontend
- name: Setup Node
  uses: actions/setup-node@v3
  with:
    node-version: '18'

- name: Install dependencies
  run: |
    cd Experiment-16/frontend
    npm install

- name: Run frontend tests (safe)
  run: |
    cd Experiment-16/frontend
    npm test || npx jest